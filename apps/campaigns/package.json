{
    "name": "campaigns",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "npx nuxt build",
        "dev": "npx nuxt dev",
        "generate": "npx npx nuxt generate",
        "preview": "npx nuxt preview",
        "postinstall": "npx nuxt prepare"
    },
    "nx": {
        "targets": {
            "build": {
                "outputs": [
                    "{projectRoot}/.output",
                    "{projectRoot}/.nuxt"
                ]
            }
        },
        "tags": [
            "scope:campaigns"
        ],
        "implicitDependencies": [
            "tag:scope:campaigns"
        ]
    }
}
